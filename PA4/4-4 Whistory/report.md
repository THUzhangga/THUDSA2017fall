1. 所使用数据结构与算法的构思、原理和实现要点。
  hash table，基于dictionary和bitmap

2. 完成过程中遇到的问题，排除问题的主要过程、使用的方法和技巧，以及参考资料。
  首先让车轮转两圈，那么能涵盖该车轮所有的印记（n）种。对于该2n长度的字符串，起始点从0到n，依次计算长度为n的字符串的hash code。为了快速计算，我用了多项式法，首尾增删即可。（其中还用到了快速幂取模）。对于一个车轮，如果它本身的hash code无占用，那么它所有的印记的hash code都要标记。那么再出现用一车轮的立刻可以发现，如果非同一车轮再标记它所有hash code。

  从结果来看，通过的结果不到一半，可能是hash function离散程度并不好。可惜没有时间进一步完善了。

3. 时间和空间复杂度的估算。

   对于2n长的两圈车轮hash code时间为$O(n)$。每次比较都是常数时间。若共有$k$条不同车轮，则复杂度为$O(nk)$。

4. （可选）介绍理论分析与实测效果的吻合程度，不吻合时进一步解释原因。

5. （可选）所用方法的特别、新颖或创新之处。